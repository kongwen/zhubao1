// Generated by CoffeeScript 1.9.2
(function() {
  var e, greeting, greetings, hi, odd, odds, squadron, xWing;

  console.log((function() {
    return 'hello ,functions';
  })());

  hi = function(a) {
    return 'hello ,functions' + a;
  };

  console.log(hi(555));

  greeting = function(subject) {
    return "hello ," + subject + "!";
  };

  console.log(greeting('arguments'));

  squadron = 'Red';

  xWing = squadron + 5;

  console.log(xWing);

  greetings = function() {
    return "hello ," + arguments[1] + "!";
  };

  console.log(greetings('111', '222'));

  odd = function(num) {
    if (typeof num === 'number') {
      if (num === Math.round(num)) {
        if (num > 0) {
          return num % 2 === 1;
        } else {
          throw num + " 不是正整数";
        }
      } else {
        throw num + " 不是整数";
      }
    } else {
      throw num + " 不是数字";
    }
  };

  console.log(odd(3));

  try {
    odd(5.1);
  } catch (_error) {
    e = _error;
    console.log(e);
  }

  odds = function(num) {
    if (typeof num !== 'number') {
      throw num + " 不是数字";
    }
    if (num !== Math.round(num)) {
      throw num + " 不是整数";
    }
    if (!(num > 0)) {
      throw num + " 不是正整数";
    }
    return num % 2 === 1;
  };

}).call(this);

//# sourceMappingURL=one2.js.map
